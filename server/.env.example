# Copy this file to .env and fill in your own values.
# Never commit the real .env file to GitHub.

# Port the Express server will run on (Render sets this automatically)
PORT=5000

# Your MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/airbnb-clone?retryWrites=true&w=majority

# A long random string used to sign JWT tokens
JWT_SECRET=replace_this_with_a_long_random_string

# How long a JWT stays valid for
JWT_EXPIRES_IN=7d

# The URLs your frontend apps run on, used for CORS
# Development
CLIENT_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174

# Production (update these if you redeploy to new domains)
# CLIENT_URL=https://airbnb-clone-omega-wine.vercel.app
# ADMIN_URL=https://airbnb-clone-qw8t.vercel.app
